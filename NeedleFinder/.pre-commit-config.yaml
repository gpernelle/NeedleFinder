default_language_version:
    python: python3.9
repos:


# avoid pushing large files, fix trailing spaces and missing line at end of file, avoid committing credentials
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v3.3.0
    hooks:
    -   id: check-added-large-files
        args: ['--maxkb=10000']
    -   id: trailing-whitespace
    -   id: mixed-line-ending
    -   id: check-json
    -   id: check-yaml
    -   id: end-of-file-fixer
    -   id: detect-aws-credentials
        args: [--allow-missing-credentials]
    -   id: detect-private-key
    -   id: requirements-txt-fixer
    -   id: check-merge-conflict
    #-   id: pretty-format-json
    #-   id: no-commit-to-branch
    #    args: [--branch, develop, --branch, master]

# changes old-style syntax to new-style syntax.
-   repo: https://github.com/asottile/pyupgrade
    rev: v3.3.1
    hooks:
    -   id: pyupgrade
        args: [--py39-plus]

## GP 2020.11.24 disable for now, it makes sometimes strange choices in the sorting
## order (cf tabulate set after functions imports)
## can be run manually with: pre-commit run isort --all-files
## sort the imports
#-   repo: https://github.com/asottile/seed-isort-config
#    rev: v2.2.0
#    hooks:
#    -   id: seed-isort-config
#-   repo: https://github.com/pre-commit/mirrors-isort
#    rev: v5.6.4
#    hooks:
#    -   id: isort
#        args: [--filter-files]
#        additional_dependencies: [toml]

# fix the formatting
-   repo: https://github.com/psf/black
    rev: 22.12.0
    hooks:
    -   id: black
-   repo: https://github.com/asottile/blacken-docs
    rev: v1.8.0
    hooks:
    -   id: blacken-docs
        additional_dependencies: [black==20.8b1]

## linter check flake8 - need some manual work before it's ready
#-   repo: https://gitlab.com/PyCQA/flake8
#    rev: 3.8.4
#    hooks:
#    -   id: flake8
